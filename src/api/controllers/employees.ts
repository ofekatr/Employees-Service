const createtEmployee = (_, res) => res.send(true)
const updateEmployee = (_, res) => res.send(true)
const readEmployee = (_, res) => res.send(true)
const deleteEmployee = (_, res) => res.send(true)

export default {
    createtEmployee,
    updateEmployee,
    readEmployee,
    deleteEmployee,
}